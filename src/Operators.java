public class Operators {
    /*
    increment
    pre-increment = varName++;
    post-increment = ++varName;
    Arithmetic operator
    //add +
    //subtract -
    //multiply *
    //divide /
    //remainder %
    //relational operators
    //< > <= >=
    //equality
    // == equals to
    // != not equals to
    Logical operators
    //logical and && - returns true if all conditions are true
    //logical or || - requires only one condition to be true
    //logical not !\

    Assignment operators
    // = += *= -= /=
     */
}
